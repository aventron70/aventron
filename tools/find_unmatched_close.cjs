const fs=require('fs');const code=fs.readFileSync('assets/js/solar-water-heater-configurator.js','utf8');let open=0;let inSingle=false,inDouble=false,inTemplate=false,inLine=false,inBlock=false,esc=false;for(let i=0;i<code.length;i++){const c=code[i];const next=code[i+1];if(esc){esc=false;continue;}if(inLine){ if(c==='\n'){ inLine=false;} continue;}if(inBlock){ if(c==='*' && next==='/' ){ inBlock=false; i++; continue;} continue;}if(!inSingle && !inDouble && !inTemplate){ if(c==='/' && next==='/' ){ inLine=true; i++; continue;} if(c==='/' && next==='*'){ inBlock=true; i++; continue;} } if(c==='\\'){ esc=true; continue;} if(!inSingle && !inDouble && !inTemplate){ if(c==='`'){ inTemplate=true; continue;} if(c==='"'){ inDouble=true; continue;} if(c==="'"){ inSingle=true; continue;} if(c==='{'){ open++; } if(c==='}'){ open--; if(open<0){ const before=code.slice(Math.max(0,i-200),i+200); const linesBefore=code.slice(0,i).split('\n'); console.log('UNMATCHED_CLOSE_AT index',i,'line',linesBefore.length+1); console.log('---CONTEXT---\n'+before); process.exit(0);} } continue;} if(inTemplate){ if(c==='`'){ inTemplate=false; continue;} if(c==='\\'){ esc=true; continue;} continue;} if(inDouble){ if(c==='"'){ inDouble=false; continue;} if(c==='\\'){ esc=true; continue;} continue;} if(inSingle){ if(c==="'"){ inSingle=false; continue;} if(c==='\\'){ esc=true; continue;} continue;} } console.log('NO_UNMATCHED_CLOSE');
